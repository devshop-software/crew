{
  "name": "@devshop-software/crew",
  "version": "0.1.0",
  "description": "Project-agnostic Claude Code skills for spec → implement → qa → review → ship",
  "bin": { "crew": "scripts/cli.js" },
  "files": [
    "skills/",
    "templates/",
    "scripts/",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "engines": { "node": ">=20" },
  "keywords": ["claude-code", "claude", "skills", "agentic", "workflow", "crew"],
  "repository": {
    "type": "git",
    "url": "https://github.com/devshop-software/crew.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "public"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "license": "MIT",
  "devDependencies": {
    "semantic-release": "^24.2.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1"
  }
}
